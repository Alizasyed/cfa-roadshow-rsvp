# CFA Investors Roadshow — RSVP

Static RSVP page (`index.html`) plus one serverless function (`api/rsvp.js`). Deploy on Vercel.

- Drop the hero photo in as `hero.jpg`.
- Set `RSVP_WEBHOOK_URL` (e.g. a Google Apps Script web app that appends to a Sheet) in Vercel env vars.
